/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 46/0.1.0; intrinsic ABI: v1; catalog SHA-256: 9b5ce71ce97b9a58e53bc98541d3449020497dbf7b46b91c1da19faf5b68619f.
// LLVM source: 1cb4e3833c1919c2e6fb579a23ac0e2b22587b7e.

//! Generated CTA barrier-reduction operations.

use pliron::{
    builtin::op_interfaces::{NOpdsInterface, NResultsInterface},
    context::{Context, Ptr},
    op::Op,
    operation::Operation,
};
use pliron_derive::pliron_op;

/// Reduces predicates with AND while synchronizing all arriving CTA threads at an aligned numbered barrier.
#[pliron_op(
    name = "nvvm.barrier_cta_red_and_aligned_all",
    format,
    verifier = "succ",
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct BarrierCtaRedAndAlignedAllOp;

impl BarrierCtaRedAndAlignedAllOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

/// Reduces predicates with AND while synchronizing the requested number of CTA threads at an aligned numbered barrier.
#[pliron_op(
    name = "nvvm.barrier_cta_red_and_aligned_count",
    format,
    verifier = "succ",
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct BarrierCtaRedAndAlignedCountOp;

impl BarrierCtaRedAndAlignedCountOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

/// Reduces predicates with AND while synchronizing all arriving CTA threads at a numbered barrier.
#[pliron_op(
    name = "nvvm.barrier_cta_red_and_all",
    format,
    verifier = "succ",
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct BarrierCtaRedAndAllOp;

impl BarrierCtaRedAndAllOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

/// Reduces predicates with AND while synchronizing the requested number of CTA threads at a numbered barrier.
#[pliron_op(
    name = "nvvm.barrier_cta_red_and_count",
    format,
    verifier = "succ",
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct BarrierCtaRedAndCountOp;

impl BarrierCtaRedAndCountOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

/// Reduces predicates with OR while synchronizing all arriving CTA threads at an aligned numbered barrier.
#[pliron_op(
    name = "nvvm.barrier_cta_red_or_aligned_all",
    format,
    verifier = "succ",
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct BarrierCtaRedOrAlignedAllOp;

impl BarrierCtaRedOrAlignedAllOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

/// Reduces predicates with OR while synchronizing the requested number of CTA threads at an aligned numbered barrier.
#[pliron_op(
    name = "nvvm.barrier_cta_red_or_aligned_count",
    format,
    verifier = "succ",
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct BarrierCtaRedOrAlignedCountOp;

impl BarrierCtaRedOrAlignedCountOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

/// Reduces predicates with OR while synchronizing all arriving CTA threads at a numbered barrier.
#[pliron_op(
    name = "nvvm.barrier_cta_red_or_all",
    format,
    verifier = "succ",
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct BarrierCtaRedOrAllOp;

impl BarrierCtaRedOrAllOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

/// Reduces predicates with OR while synchronizing the requested number of CTA threads at a numbered barrier.
#[pliron_op(
    name = "nvvm.barrier_cta_red_or_count",
    format,
    verifier = "succ",
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct BarrierCtaRedOrCountOp;

impl BarrierCtaRedOrCountOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

/// Counts true predicates while synchronizing all arriving CTA threads at an aligned numbered barrier.
#[pliron_op(
    name = "nvvm.barrier_cta_red_popc_aligned_all",
    format,
    verifier = "succ",
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct BarrierCtaRedPopcAlignedAllOp;

impl BarrierCtaRedPopcAlignedAllOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

/// Counts true predicates while synchronizing the requested number of CTA threads at an aligned numbered barrier.
#[pliron_op(
    name = "nvvm.barrier_cta_red_popc_aligned_count",
    format,
    verifier = "succ",
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct BarrierCtaRedPopcAlignedCountOp;

impl BarrierCtaRedPopcAlignedCountOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

/// Counts true predicates while synchronizing all arriving CTA threads at a numbered barrier.
#[pliron_op(
    name = "nvvm.barrier_cta_red_popc_all",
    format,
    verifier = "succ",
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct BarrierCtaRedPopcAllOp;

impl BarrierCtaRedPopcAllOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

/// Counts true predicates while synchronizing the requested number of CTA threads at a numbered barrier.
#[pliron_op(
    name = "nvvm.barrier_cta_red_popc_count",
    format,
    verifier = "succ",
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct BarrierCtaRedPopcCountOp;

impl BarrierCtaRedPopcCountOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

pub(super) fn register(ctx: &mut Context) {
    BarrierCtaRedAndAlignedAllOp::register(ctx);
    BarrierCtaRedAndAlignedCountOp::register(ctx);
    BarrierCtaRedAndAllOp::register(ctx);
    BarrierCtaRedAndCountOp::register(ctx);
    BarrierCtaRedOrAlignedAllOp::register(ctx);
    BarrierCtaRedOrAlignedCountOp::register(ctx);
    BarrierCtaRedOrAllOp::register(ctx);
    BarrierCtaRedOrCountOp::register(ctx);
    BarrierCtaRedPopcAlignedAllOp::register(ctx);
    BarrierCtaRedPopcAlignedCountOp::register(ctx);
    BarrierCtaRedPopcAllOp::register(ctx);
    BarrierCtaRedPopcCountOp::register(ctx);
}

/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 46/0.1.0; intrinsic ABI: v1; catalog SHA-256: 9b5ce71ce97b9a58e53bc98541d3449020497dbf7b46b91c1da19faf5b68619f.
// LLVM source: 1cb4e3833c1919c2e6fb579a23ac0e2b22587b7e.

// Included inside `cuda_device::barrier`.

/// Reduces predicates with AND while synchronizing all arriving CTA threads at an aligned numbered barrier.
///
/// # Safety
/// All participating threads must use a compatible barrier protocol.
#[inline(never)]
pub unsafe fn barrier_cta_red_and_aligned_all(barrier_id: u32, predicate: bool) -> bool {
    let _ = (barrier_id, predicate);
    unreachable!("barrier_cta_red_and_aligned_all called outside CUDA kernel context")
}

/// Reduces predicates with AND while synchronizing the requested number of CTA threads at an aligned numbered barrier.
///
/// # Safety
/// All participating threads must use a compatible barrier protocol.
#[inline(never)]
pub unsafe fn barrier_cta_red_and_aligned_count(
    barrier_id: u32,
    thread_count: u32,
    predicate: bool,
) -> bool {
    let _ = (barrier_id, predicate);
    let _ = thread_count;
    unreachable!("barrier_cta_red_and_aligned_count called outside CUDA kernel context")
}

/// Reduces predicates with AND while synchronizing all arriving CTA threads at a numbered barrier.
///
/// # Safety
/// All participating threads must use a compatible barrier protocol.
#[inline(never)]
pub unsafe fn barrier_cta_red_and_all(barrier_id: u32, predicate: bool) -> bool {
    let _ = (barrier_id, predicate);
    unreachable!("barrier_cta_red_and_all called outside CUDA kernel context")
}

/// Reduces predicates with AND while synchronizing the requested number of CTA threads at a numbered barrier.
///
/// # Safety
/// All participating threads must use a compatible barrier protocol.
#[inline(never)]
pub unsafe fn barrier_cta_red_and_count(
    barrier_id: u32,
    thread_count: u32,
    predicate: bool,
) -> bool {
    let _ = (barrier_id, predicate);
    let _ = thread_count;
    unreachable!("barrier_cta_red_and_count called outside CUDA kernel context")
}

/// Reduces predicates with OR while synchronizing all arriving CTA threads at an aligned numbered barrier.
///
/// # Safety
/// All participating threads must use a compatible barrier protocol.
#[inline(never)]
pub unsafe fn barrier_cta_red_or_aligned_all(barrier_id: u32, predicate: bool) -> bool {
    let _ = (barrier_id, predicate);
    unreachable!("barrier_cta_red_or_aligned_all called outside CUDA kernel context")
}

/// Reduces predicates with OR while synchronizing the requested number of CTA threads at an aligned numbered barrier.
///
/// # Safety
/// All participating threads must use a compatible barrier protocol.
#[inline(never)]
pub unsafe fn barrier_cta_red_or_aligned_count(
    barrier_id: u32,
    thread_count: u32,
    predicate: bool,
) -> bool {
    let _ = (barrier_id, predicate);
    let _ = thread_count;
    unreachable!("barrier_cta_red_or_aligned_count called outside CUDA kernel context")
}

/// Reduces predicates with OR while synchronizing all arriving CTA threads at a numbered barrier.
///
/// # Safety
/// All participating threads must use a compatible barrier protocol.
#[inline(never)]
pub unsafe fn barrier_cta_red_or_all(barrier_id: u32, predicate: bool) -> bool {
    let _ = (barrier_id, predicate);
    unreachable!("barrier_cta_red_or_all called outside CUDA kernel context")
}

/// Reduces predicates with OR while synchronizing the requested number of CTA threads at a numbered barrier.
///
/// # Safety
/// All participating threads must use a compatible barrier protocol.
#[inline(never)]
pub unsafe fn barrier_cta_red_or_count(
    barrier_id: u32,
    thread_count: u32,
    predicate: bool,
) -> bool {
    let _ = (barrier_id, predicate);
    let _ = thread_count;
    unreachable!("barrier_cta_red_or_count called outside CUDA kernel context")
}

/// Counts true predicates while synchronizing all arriving CTA threads at an aligned numbered barrier.
///
/// # Safety
/// All participating threads must use a compatible barrier protocol.
#[inline(never)]
pub unsafe fn barrier_cta_red_popc_aligned_all(barrier_id: u32, predicate: bool) -> u32 {
    let _ = (barrier_id, predicate);
    unreachable!("barrier_cta_red_popc_aligned_all called outside CUDA kernel context")
}

/// Counts true predicates while synchronizing the requested number of CTA threads at an aligned numbered barrier.
///
/// # Safety
/// All participating threads must use a compatible barrier protocol.
#[inline(never)]
pub unsafe fn barrier_cta_red_popc_aligned_count(
    barrier_id: u32,
    thread_count: u32,
    predicate: bool,
) -> u32 {
    let _ = (barrier_id, predicate);
    let _ = thread_count;
    unreachable!("barrier_cta_red_popc_aligned_count called outside CUDA kernel context")
}

/// Counts true predicates while synchronizing all arriving CTA threads at a numbered barrier.
///
/// # Safety
/// All participating threads must use a compatible barrier protocol.
#[inline(never)]
pub unsafe fn barrier_cta_red_popc_all(barrier_id: u32, predicate: bool) -> u32 {
    let _ = (barrier_id, predicate);
    unreachable!("barrier_cta_red_popc_all called outside CUDA kernel context")
}

/// Counts true predicates while synchronizing the requested number of CTA threads at a numbered barrier.
///
/// # Safety
/// All participating threads must use a compatible barrier protocol.
#[inline(never)]
pub unsafe fn barrier_cta_red_popc_count(
    barrier_id: u32,
    thread_count: u32,
    predicate: bool,
) -> u32 {
    let _ = (barrier_id, predicate);
    let _ = thread_count;
    unreachable!("barrier_cta_red_popc_count called outside CUDA kernel context")
}

[package]
name = "host_closure"
version = "0.1.0"
edition = "2024"
authors = ["Nihal Pasham <nihalp@nvidia.com>"]
license = "Apache-2.0"

[workspace]

[dependencies]
cuda-device = { path = "../../../cuda-device" }
cuda-host = { path = "../../../cuda-host", features = ["async"] }
cuda-core = { git = "https://github.com/NVlabs/cutile-rs", tag = "v0.3.1" }
cuda-async = { git = "https://github.com/NVlabs/cutile-rs", tag = "v0.3.1" }

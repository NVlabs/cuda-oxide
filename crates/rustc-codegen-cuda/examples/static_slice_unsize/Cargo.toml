[package]
name = "static_slice_unsize"
version = "0.1.0"
edition = "2024"
publish = false

# Zero-addend device-static array→slice unsize.
#   cargo oxide run static_slice_unsize

[dependencies]
cuda-device = { path = "../../../cuda-device" }
cuda-host = { path = "../../../cuda-host" }
cuda-core = { path = "../../../cuda-core" }

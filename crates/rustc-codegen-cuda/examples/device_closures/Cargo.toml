[package]
name = "device_closures"
version = "0.1.0"
edition = "2024"
authors = ["Nihal Pasham <nihalp@nvidia.com>"]
license = "Apache-2.0"

# This is a standalone workspace member for unified compilation testing
[workspace]

[dependencies]
cuda-device = { path = "../../../cuda-device" }
cuda-core = { git = "https://github.com/NVlabs/cutile-rs", branch = "feat/cuda-oxide-host-support" }
cuda-host = { path = "../../../cuda-host" }

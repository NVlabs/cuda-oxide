[package]
name = "len_test"
version = "0.1.0"
edition = "2024"
authors = ["Deepak"]
license = "Apache-2.0"

[workspace]

[dependencies]
cuda-device = { path = "../../../cuda-device" }
cuda-host = { path = "../../../cuda-host" }
cuda-core = { path = "../../../cuda-core" }

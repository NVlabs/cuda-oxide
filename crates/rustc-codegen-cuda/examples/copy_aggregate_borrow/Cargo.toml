[package]
name = "copy_aggregate_borrow"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[workspace]

# Regression example for issue #400.
#
# Run with:
#   CUDA_OXIDE_MIR_OPTS=copy-aggregate-borrows \
#     cargo oxide run copy_aggregate_borrow

[dependencies]
cuda-device = { path = "../../../cuda-device" }
cuda-host = { path = "../../../cuda-host" }
cuda-core = { path = "../../../cuda-core" }

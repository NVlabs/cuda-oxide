[package]
name = "tuple_array_provenance"
version = "0.1.0"
edition = "2024"
authors = ["NVIDIA CORPORATION"]
license = "Apache-2.0"

[workspace]

# Array-of-tuple constant thin-pointer provenance to device statics.
#   cargo oxide run tuple_array_provenance

[dependencies]
cuda-core = { git = "https://github.com/NVlabs/cutile-rs", branch = "main" }
cuda-device = { path = "../../../cuda-device" }
cuda-host = { path = "../../../cuda-host" }

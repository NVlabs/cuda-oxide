[package]
name = "slice_range"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[workspace]

# Reproducer + regression test for mir-importer limitation:
# `Aggregate kind RawPtr(...)` is unsupported.
#
# Build with:
#   cargo oxide run slice_range

[dependencies]
cuda-device = { path = "../../../cuda-device" }
cuda-host = { path = "../../../cuda-host" }
cuda-core = { path = "../../../cuda-core" }
